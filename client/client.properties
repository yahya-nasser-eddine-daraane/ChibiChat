# ChibiChat — Client Configuration
server.url=http://localhost:8080

# Use Windows Authentication for local history
local.db.host=localhost
local.db.port=1433
local.db.username=
local.db.password=
# We will use integratedSecurity=true in the URL if possible, 
# but for now let's just leave it blank and see if it fails gracefully.
